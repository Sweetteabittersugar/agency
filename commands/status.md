检查 maestro agent 状态：
- `@status` → 查看所有 agent 任务状态
- `@tracker` → 查看任务看板
- `python $PROJECT_ROOT/maestro/dispatch.py --status` → 最近 10 个任务
- `python $PROJECT_ROOT/maestro/task-tracker.py --list` → 任务列表
