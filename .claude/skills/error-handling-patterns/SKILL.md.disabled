---
name: error-handling-patterns
description: 错误处理模式 —— 异常分类体系、重试策略、优雅降级与错误传播链
category: implementation
loading: on-demand
triggers:
  keywords: ["错误处理", "异常处理", "error handling", "重试", "降级"]
---

# 这是什么

错误处理是软件可靠性的基石。本 Skill 提供一套系统化的错误处理框架，涵盖异常分类、重试策略、优雅降级和服务恢复。

# 何时使用

- 设计新服务的错误处理体系
- 现有系统频繁出现未捕获异常
- 需要提升服务可用性（SLA 从 99% 到 99.9%）
- 微服务间调用链经常中断
- 错误日志噪音大，难以定位真正问题

# 核心规则

1. **分类优先于处理**：先给错误分类（可恢复 vs 不可恢复、临时 vs 永久），不同类别走不同处理路径。
2. **快速失败，安全恢复**：在系统边界快速校验并拒绝无效请求。内部错误重试时要有退避策略，避免雪崩。
3. **错误信息分层**：用户看到友好提示，开发者看到堆栈和上下文，运维看到聚合统计。三个层级不互相泄露。
4. **重试不是万能的**：只对临时性错误（网络抖动、锁冲突、服务暂时不可用）重试。对业务逻辑错误（参数无效、权限不足）重试是浪费资源。
5. **降级优于崩溃**：核心路径失败时，提供有限功能让用户继续操作，而非直接报错。

# 工作流程

1. **错误分类**
   - **可恢复错误**：网络超时、临时服务不可用、死锁重试 —— 可以重试
   - **不可恢复错误**：参数校验失败、认证失败、资源不存在 —— 直接返回错误
   - **系统错误**：内存不足、磁盘满、端口占用 —— 触发告警，优雅关闭
   - **业务错误**：库存不足、余额不够、状态不允许 —— 返回业务错误码

2. **重试策略**
   - **固定间隔**：每次重试等固定时间（简单但不优雅）
   - **指数退避**：1s → 2s → 4s → 8s，加随机抖动防雷群效应
   - **指数退避 + 上限**：同上但设定最大等待时间
   - **熔断器模式**：连续失败 N 次后直接拒绝请求一段时间，快速失败

3. **降级策略**
   - 功能降级：关闭非核心功能，保证核心流程
   - 数据降级：返回缓存数据替代实时数据（标注"数据可能延迟"）
   - 服务降级：主服务不可用时切换到备用服务
   - UI 降级：展示骨架屏或简化版界面

4. **错误传播**
   - 每层添加自己的上下文后向上传播：`raise PaymentError(f"订单 {order_id} 支付失败") from original_error`
   - 在系统边界（API 层）统一转换为 HTTP 状态码
   - 不在传播链中吞掉原始异常——除非你能完全处理它

5. **监控与告警**
   - 按错误类型统计发生率
   - 设置告警阈值：错误率 > 1% 触发告警
   - 关键路径的每个错误必须记录完整上下文（用户ID、请求ID、参数）
   - 区分用户错误（4xx）和服务错误（5xx）

# 熔断器状态机

```
CLOSED → (失败N次) → OPEN → (等待超时) → HALF_OPEN → (测试成功) → CLOSED
                                              ↓ (测试失败)
                                            OPEN (重新计时)
```

# 常见错误处理反模式

| 反模式 | 问题 | 正确做法 |
|--------|------|----------|
| `catch (Exception e) {}` | 吞掉所有错误，系统静默失效 | 至少记录日志，决定是重试还是传递 |
| `throw new Error(e.message)` | 丢失原始堆栈 | 用 `from original_error` 保留原因链 |
| 所有地方都 try-catch | 掩盖真正问题 | 在系统边界统一处理，内部让错误自然传播 |
| 重试 10 次 | 用户长时间等待 | 最多 3 次，超出返回错误让用户决定 |
| 错误消息直接展示给用户 | 泄露内部信息 | 展示友好提示，记录技术细节到日志 |

# 各语言错误处理惯用法

- **Python**：`try/except` 分层，`raise ... from` 保留原因链，自定义异常类继承
- **Go**：返回 `(result, error)`，用 `fmt.Errorf("context: %w", err)` 包装
- **JavaScript**：`try/catch` + `async/await`，自定义 Error 子类
- **Java**：检查型异常（需声明）vs 非检查型异常（运行时），`try-with-resources`

# 参考标准

- Release It!（Michael Nygard）—— 分布式系统稳定性模式
- Circuit Breaker Pattern（Martin Fowler）—— 熔断器模式详解
- Google SRE Book —— 错误预算和服务可靠性
