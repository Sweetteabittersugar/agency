---
name: rag-patterns
description: RAG 模式方法论 —— RAG 架构设计、文档切分策略、检索优化与重排序
category: orchestration
loading: on-demand
triggers:
  keywords: ["RAG", "检索增强", "向量检索", "知识库"]
---

# 这是什么

检索增强生成（RAG）是将信息检索与 AI 生成相结合的技术——先从知识库中检索相关文档，再将文档作为上下文提供给模型，生成更准确的回答。

# 何时使用

- 构建基于私有知识的问答系统
- 需要模型回答超出训练数据范围的最新信息
- 减少模型幻觉，让回答有据可查
- 为不同用户群提供定制化知识库
- 替代需要频繁微调的专业领域问答

# 核心规则

1. **垃圾进垃圾出**：文档质量决定 RAG 效果。低质量、不完整、混乱的文档即使检索到也无用。
2. **切分比检索更重要**：文档如何切分直接影响检索的准确性。切得太碎丢失上下文，切得太大检索不精准。
3. **检索不等于理解**：检索到的文档可能不相关或矛盾。让模型判断文档的相关性和可靠性，而非盲目信任。
4. **混合检索优于单一方法**：向量检索 + 关键词检索（BM25）结合比纯向量检索更可靠。
5. **评估要量化**：不要凭感觉判断 RAG 效果。用忠实度、相关性、覆盖率等可量化指标评估。

# 工作流程

1. **文档预处理**
   - 格式标准化：PDF、HTML、Markdown → 纯文本
   - 清洗：去除页眉页脚、水印、HTML 标签
   - 结构保留：保留标题层级、列表结构、表格信息
   - 元数据提取：来源、章节、更新时间、标签

2. **文档切分策略**
   - **固定长度切分**：每 N 个 token 切一片。最简单但不智能。
   - **语义切分**：按段落、章节自然边界切分。
   - **递归切分**：先用大分隔符（`\n\n`）分，如果分片太大再用小分隔符（`\n`、`。`）。
   - **重叠窗口**：相邻分片间 10-20% 重叠，防止信息在边界处断裂。
   - 分片大小建议：Embedding 模型的 context 限制在 512 token 左右时，分片 200-500 token 效果较好。

3. **向量化与存储**
   - Embedding 模型选择：按语言和领域选择
   - 向量数据库：ChromaDB（轻量）、Milvus（大规模）、Pinecone（托管）
   - 同时建立倒排索引用于关键词检索（BM25）
   - 元数据过滤：支持按时间、来源、类别缩小检索范围

4. **检索优化**
   - **混合检索**：向量语义相似度 + BM25 关键词匹配，结果融合
   - **重排序（Reranker）**：Cohere Rerank / BGE-Reranker 对初检结果精排
   - **查询改写**：将用户口语化问题改写为更适合检索的形式
   - **多轮检索**：第一轮粗检 → 用结果再搜 → 第二轮精检
   - **父文档检索**：用小分片检索定位，返回对应的大分片作为上下文

5. **生成与验证**
   - 引用标注：生成的回答中标注每段信息的来源
   - 置信度评估：低置信度时明确告知用户"信息可能不完整"
   - 无答案处理：检索不到相关内容时诚实回答"未找到相关信息"，不编造
   - 来源可见：提供原始文档链接供用户验证

# RAG 架构模式

```
用户问题 → 查询改写 → 混合检索 → 重排序 → Top K 文档
                                                 ↓
                    最终回答 ← LLM 生成 ← Prompt 模板 + 文档
```

# 评估指标

| 指标 | 衡量内容 | 计算方法 |
|------|---------|---------|
| 忠实度（Faithfulness） | 回答是否基于检索文档 | 人工/LLM 评估回答与文档的一致性 |
| 答案相关性 | 回答是否回答了问题 | 评估回答与问题的语义相关性 |
| 上下文召回率 | 是否检索到了相关文档 | 检出的相关文档 / 所有相关文档 |
| 上下文精确率 | 检索结果是否相关 | 检出的相关文档 / 检出文档总数 |

# 常见问题与解决

| 问题 | 原因 | 解决 |
|------|------|------|
| 检索到不相关文档 | 切分太大/太小 | 调整切分策略，加重排序 |
| 遗漏关键信息 | 切分位置在关键信息处 | 重叠窗口，语义切分 |
| 回答不一致 | 检索到矛盾信息 | 提示词中加交叉验证指令 |
| 响应太慢 | 检索链路太长 | 缓存常见查询结果 |

# 参考标准

- LangChain / LlamaIndex 文档 —— RAG 框架最佳实践
- Anthropic Contextual Retrieval —— 上下文检索提升 RAG 效果
- MTEB 排行榜 —— Embedding 模型性能对比
- BEIR 基准 —— 检索模型评估标准
