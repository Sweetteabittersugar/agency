---
name: mock-strategy
description: Mock 策略 —— Mock vs Stub vs Fake 的区别、何时 mock、mock 框架选择
category: testing
loading: on-demand
triggers:
  keywords: ["Mock", "模拟数据", "测试替身", "打桩"]
---

# 这是什么

Mock（测试替身）用于在测试中替换真实依赖，使测试快速、可重复且不受外部环境影响。本 Skill 帮你区分各类测试替身并做出正确选择。

# 何时使用

- 依赖的外部 API 不稳定或不可达
- 数据库操作需要特定数据但难以构造
- 时间相关的逻辑需要精确控制
- 需要模拟错误场景（网络故障、超时、服务不可用）
- 测试需要验证依赖的具体调用方式

# 核心规则

1. **优先用真实对象，Mock 是最后手段**：能用值对象就不用 Mock。Mock 会增加测试与实现的耦合。
2. **Mock 类型，不 Mock 数据**：Mock 数据库驱动而不是 Mock 每一行数据。Mock 应该是"这个服务返回什么"而不是"这个函数第3个参数是什么"。
3. **不要 Mock 你不拥有的代码**：不 Mock 第三方库的内部实现。封装第三方依赖到一个薄接口，然后 Mock 你的接口。
4. **状态验证 > 行为验证**：更关心"操作后的状态是否正确"，而不是"某个方法被调用了3次"。
5. **每个测试最多 Mock 3 个对象**：超过说明被测试单元职责太多，需要拆分。

# 工作流程

1. **识别测试障碍**
   - 列出被测代码的所有外部依赖
   - 区分哪些依赖使测试变慢、不稳定或不可控
   - 只对实际造成问题的依赖使用替身

2. **选择替身类型**
   - **Dummy**：只传引用从不使用。用于填充参数列表。
   - **Stub**：返回预设值的简单替身。不记录调用。
   - **Spy**：记录调用信息（次数、参数）的真实对象包装器。
   - **Mock**：预设期望的调用模式，不匹配则失败。
   - **Fake**：真实但简化的实现（内存数据库代替 MySQL）。

3. **实施原则**
   - 在构造函数或方法参数中注入依赖（依赖注入）
   - 定义接口或协议，让真实实现和 Mock 实现遵循同一契约
   - Mock 的返回值应是合理的真实数据快照，而非 `"foo"` 之类无意义值
   - 测试结束时重置 Mock 状态，避免测试间干扰

4. **Mock 框架选择**
   - Python：`unittest.mock`（标准库）、`pytest-mock`
   - JavaScript：`jest.fn()`（内置）、`sinon`
   - Go：`gomock`、`testify/mock`
   - Java：`Mockito`（标准）、`EasyMock`

# 测试替身速查表

| 类型 | 有返回值 | 记录调用 | 验证调用 | 使用场景 |
|------|---------|---------|---------|---------|
| Dummy | 否 | 否 | 否 | 填充参数 |
| Stub | 是 | 否 | 否 | 提供预设数据 |
| Spy | 是 | 是 | 否 | 观察调用 |
| Mock | 是 | 是 | 是 | 验证交互 |
| Fake | 是 | 否 | 否 | 轻量级替代 |

# 常见 Mock 场景

| 场景 | Mock 方式 | 示例 |
|------|----------|------|
| HTTP API | Mock 请求库返回预设响应 | `mock.patch('requests.get')` |
| 数据库 | 用 SQLite 内存数据库 | `CREATE TABLE ...` 在内存中 |
| 时间 | Monkey-patch `datetime.now()` | 返回固定时间戳 |
| 文件系统 | 用 `tmpdir` 和 `StringIO` | 临时文件 |
| 环境变量 | Monkey-patch `os.environ` | 设置测试专用值 |
| 随机数 | Mock `random()` 返回固定值 | 确定性测试 |

# Mock 过度使用信号

如果你的测试出现以下情况，说明 Mock 过度：
- 看不懂测试在测什么（Mock 太复杂）
- 重构代码时大量测试失败但功能正常（Mock 与实现过于耦合）
- Mock 设置的代码行数超过实际测试逻辑
- 测试 Mock 了被测试对象本身的内部方法

# 参考标准

- xUnit 测试模式 —— 测试替身分类
- Mockito 文档 —— Mock 框架最佳实践
- Martin Fowler "Mocks Aren't Stubs" —— 经典区分文章
