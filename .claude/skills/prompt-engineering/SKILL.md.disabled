---
name: prompt-engineering
description: 提示词工程方法论 —— 提示词设计原则、Chain-of-Thought、Few-shot 策略与模板化
category: orchestration
loading: on-demand
triggers:
  keywords: ["提示词", "prompt", "prompt engineering", "提示工程"]
---

# 这是什么

提示词工程是设计和优化输入给 AI 模型的文本指令，以获取更准确、可靠和可控输出的实践方法。

# 何时使用

- 设计 Agent 或智能助手的系统提示词
- 对 AI 输出质量不满意，需要优化效果
- 将复杂的推理任务分解为可管理的步骤
- 需要 AI 以特定格式（JSON、表格、代码）输出
- 处理 AI 幻觉或不稳定输出问题

# 核心规则

1. **明确比隐晦好**：不要暗示或期望 AI "理解你的意图"。把格式、范围、限制说得一清二楚。少写"请你帮我……"，多写"给定以下信息，输出一个 JSON 对象……"。
2. **给例子比讲道理有效**：Few-shot 示例比长篇文字描述更能让模型理解你想要的输出格式和风格。
3. **引导推理过程**：要求模型"一步步思考"或"先分析再回答"，能显著提升复杂问题的准确率。
4. **约束不缩小能力**：合理约束（格式、长度、风格）能提升输出质量。过度约束（不允许任何解释）反而降低质量。
5. **提示词是代码，需要版本管理**：像对待代码一样对待提示词。测试、迭代、对比、记录。

# 工作流程

1. **需求分析**
   - 明确任务类型：生成、分类、提取、推理、翻译
   - 定义输出格式：纯文本、JSON、表格、代码块
   - 确定质量标准和评估方法
   - 了解模型的能力边界

2. **提示词设计**
   - **角色设定**（Persona）：`你是一个资深的 Python 工程师`——设定专业领域
   - **任务描述**（Task）：`审查以下代码中的安全漏洞`——明确做什么
   - **上下文信息**（Context）：提供必要的背景数据
   - **输出格式**（Format）：`以 JSON 格式返回，包含 severity 和 suggestion 字段`
   - **约束条件**（Constraints）：`不要包含密码学建议`、`用中文回答`

3. **高级策略**
   - **Chain-of-Thought（CoT）**：要求"逐步推理"，把隐性思维过程显性化
   - **Few-shot**：提供 3-5 个高质量示例，用示例展示期望的行为
   - **ReAct**：Reasoning + Acting，交替推理和行动步骤
   - **自洽性（Self-Consistency）**：多次采样取多数结果，提高稳定性
   - **角色扮演**：设定具体角色和专业背景，限制输出的知识范围

4. **迭代优化**
   - 先用少量示例（3-5 个）测试提示词
   - 分析输出中的错误模式
   - 针对性添加约束或示例
   - 对比不同提示词版本的输出质量
   - 记录每次改进的理由

5. **工程化**
   - 提示词模板化：用变量替代可变化的部分
   - 版本管理：提示词纳入 Git，每次修改有 commit
   - A/B 测试：对比新旧提示词的效果
   - 监控：追踪输出质量和失败率

# 提示词模板示例

```
## 角色
你是一个 {role}，专注于 {domain}。

## 任务
{task_description}

## 输出格式
{format_specification}

## 规则
1. {rule_1}
2. {rule_2}

## 示例
输入: {example_input}
输出: {example_output}

## 待处理
{user_input}
```

# 常见反模式

| 反模式 | 问题 | 改进 |
|--------|------|------|
| 指令过于模糊 | 模型自由发挥，结果不可控 | 明确格式、范围和约束 |
| Few-shot 示例质量差 | 模型学到错误的模式 | 只用高质量的黄金示例 |
| 提示词过长 | 模型忽略后半部分信息 | 精简到核心，或分步处理 |
| 缺乏负面约束 | 模型可能会做你不想要的事 | 明确说"不要……" |
| 过度复杂 | 指令矛盾或难以理解 | 分开步骤，每次提一个要求 |

# 各类任务提示词策略

| 任务类型 | 推荐策略 |
|----------|---------|
| 文本生成 | 角色 + 风格描述 + 字数限制 |
| 代码生成 | 语言 + 规范 + 示例 + 测试用例 |
| 信息提取 | 列出精确字段 + JSON 格式 + 示例 |
| 分类 | 类别列表 + 决策规则 + 边界情况 |
| 推理 | CoT + 自洽性 + 验证步骤 |
| 翻译 | 目标语言 + 术语表 + 风格要求 |

# 参考标准

- OpenAI Prompt Engineering Guide —— 官方提示词最佳实践
- Anthropic Prompt Library —— 提示词模板和设计原则
- Chain-of-Thought（Wei et al.）—— 推理增强方法
- ReAct（Yao et al.）—— 推理与行动结合
