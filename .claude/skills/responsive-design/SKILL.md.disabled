---
name: responsive-design
description: 响应式设计方法论 —— 断点策略、弹性布局、图片适配与移动优先
category: frontend
loading: on-demand
triggers:
  keywords: ["响应式", "responsive", "移动端", "适配", "断点"]
---

# 这是什么

响应式设计是让 Web 页面在不同屏幕尺寸和设备上都能提供良好体验的设计方法。核心思想是用一套代码适应所有终端，而非为每种设备单独开发。

# 何时使用

- 新项目启动，需要确定响应式策略
- 现有页面在移动端体验差，需要改造
- 设计稿交付后，需要转换为响应式布局
- 用户反馈特定设备上布局错乱
- 性能审计发现移动端加载过慢

# 核心规则

1. **移动优先**：先设计最小屏幕的样式，再用媒体查询向上扩展。移动端只加载必要资源，大屏逐步增强。
2. **断点由内容决定**：不针对特定设备设断点。当内容看起来"挤"或"散"时，那就是断点位置。
3. **相对单位优先**：用 rem、em、%、vw/vh 替代固定 px。让布局随字体大小和视口等比缩放。
4. **触摸友好**：点击目标至少 44x44px，留足间距防误触。悬停状态不能是唯一的信息传达方式。
5. **性能内建**：响应式不是只改 CSS。不同设备加载不同大小的图片、不同复杂度的组件。

# 工作流程

1. **确定断点**
   - 从内容出发，拉伸/收缩浏览器找到布局"断裂点"
   - 常见断点参考：<576px 手机、576-768px 大屏手机、768-1024px 平板、>1024px 桌面
   - 用 SCSS/LESS 变量或 CSS 自定义属性统一管理断点值
   - 保持断点数量在 3-5 个，过多难以维护

2. **弹性布局选择**
   - Flexbox：一维布局（导航栏、卡片列表、表单行）
   - Grid：二维布局（页面整体结构、仪表盘、图片墙）
   - 避免固定宽度容器，用 max-width 限制内容区宽度
   - 使用 `min()` / `max()` / `clamp()` 实现流��排版

3. **图片适配**
   - `<picture>` + `srcset` 按屏幕宽度和像素密度加载不同图片
   - 使用 `sizes` 属性告知浏览器图片显示尺寸
   - 默认用 WebP/AVIF 格式，`<picture>` 内提供回退
   - 图片容器设置 `max-width: 100%` 防止溢出
   - 非首屏图片使用 `loading="lazy"` 延迟加载

4. **排版响应式**
   - 根字体用 `clamp()` 实现流式缩放：`font-size: clamp(16px, 2.5vw, 20px)`
   - 标题用 rem 或 em，保持层级比例
   - 行宽控制在 45-75 字符（用 `max-width: 65ch`）
   - 行高随字号增大而减小

5. **导航适配**
   - 大屏：水平导航栏或侧边栏
   - 小屏：汉堡菜单、底部 Tab 栏、或优先级折叠
   - 确保所有导航入口在移动端可达
   - 面包屑在大屏显示完整路径，小屏只显示"返回上级"

6. **测试验证**
   - 浏览器 DevTools 设备模拟测试常见分辨率
   - 真机测试至少覆盖 iOS Safari 和 Android Chrome
   - 横屏和竖屏都要测试
   - 测试触控操作：滑动、长按、双指缩放

# 断点策略对比

| 策略 | 优点 | 缺点 |
|------|------|------|
| 设备定向 | 针对性强 | 设备种类太多，无法穷举 |
| 内容定向 | 适应性强，未来友好 | 需要更多前期分析 |
| 混合策略 | 兼顾常见设备和内容 | 实现复杂度略高 |

推荐内容定向为主，在关键断点处参考常见设备宽度。

# CSS 容器查询

当组件被放置在不同宽度的容器中（非视口宽度变化），使用容器查询替代媒体查询：

```css
.container {
  container-type: inline-size;
}
@container (min-width: 400px) {
  .card { flex-direction: row; }
}
```

# 移动端特定考量

- **减少输入负担**：用选择器、开关替代文本输入；输入框设置合适的 type 唤起对应键盘
- **网络不确定性**：离线优先策略，关键内容缓存，提交操作防丢
- **减少动画**：移动端 CPU/GPU 资源有限，避免复杂动画。用 `prefers-reduced-motion` 尊重用户偏好
- **viewport meta**：`<meta name="viewport" content="width=device-width, initial-scale=1">`

# 参考标准

- Google 响应式设计基础 —— 移动优先的权威指南
- CSS Grid 规范 —— 二维布局标准
- Flexbox 规范 —— 一维布局标准
- WebAIM 移动端无障碍指南 —— 触控和无障碍最佳实践
