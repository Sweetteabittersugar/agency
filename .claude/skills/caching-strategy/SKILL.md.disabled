---
name: caching-strategy
description: 缓存策略方法论 —— 缓存层级设计、失效策略、Redis 模式与 CDN 缓存
category: implementation
loading: on-demand
triggers:
  keywords: ["缓存", "cache", "Redis", "失效策略"]
---

# 这是什么

缓存是提升系统性能的核心手段。本 Skill 涵盖缓存层级选择、失效策略设计、常见缓存模式以及 Redis 最佳实践。

# 何时使用

- API 响应慢，需要加速
- 数据库查询压力大，需要缓冲
- 热点数据频繁被访问
- 需要设计多级缓存架构
- 缓存数据不一致问题排查

# 核心规则

1. **缓存是权衡，不是银弹**：缓存提升读性能但增加系统复杂度、内存成本和一致性问题。只在真正需要时引入。
2. **缓存失效比缓存命中更难**：决定何时让缓存失效、如何保证一致性，比决定缓存什么更难。优先选择 TTL 自动过期，而不是手动失效。
3. **缓存穿透保护**：大量请求查询不存在的数据时，缓存空值防止穿透到数据库。
4. **热点保护**：单个 key 被密集访问时，用本地缓存或 key 分片分散压力。
5. **监控命中率**：缓存命中率是核心指标。命中率低于 80% 需要调整策略。

# 工作流程

1. **缓存层级选择**
   - **浏览器缓存**：静态资源（JS/CSS/图片），HTTP Cache-Control 头控制
   - **CDN 缓存**：边缘节点缓存，加速全球访问
   - **反向代理缓存**：Nginx/Varnish 缓存完整 HTTP 响应
   - **应用缓存**：Redis/Memcached 缓存业务数据
   - **本地缓存**：进程内存缓存，极快但容量有限

2. **失效策略设计**
   - **TTL（生存时间）**：设置固定过期时间。简单可靠，首选方案。
   - **主动失效**：数据更新时主动删除对应缓存。实时性好但容易遗漏。
   - **LRU/LFU 淘汰**：内存满时按最近最少使用/最不经常使用规则淘汰。
   - **懒失效**：访问时检查是否过期。节省资源但可能返回短暂过期的数据。

3. **缓存模式**
   - **Cache-Aside（旁路缓存）**：应用同时管理缓存和数据库。先查缓存，未命中查数据库后写入缓存。
   - **Read-Through**：缓存层代理数据库读取。应用只与缓存交互，缓存负责回源。
   - **Write-Through**：写入同时更新缓存和数据库。保证一致性但写入延迟高。
   - **Write-Behind（写后）**：先写缓存，异步批量写数据库。写入快但有数据丢失风险。

4. **Redis 数据类型与场景**
   - **String**：计数器、分布式锁、简易 KV 缓存
   - **Hash**：对象缓存（用户信息、配置项）
   - **List**：消息队列、最近 N 条记录
   - **Set**：标签、好友列表、共同关注
   - **Sorted Set**：排行榜、延迟队列、优先级排序
   - **Stream**：持久化消息队列、事件溯源

5. **缓存穿透/击穿/雪崩防护**
   - **穿透（不存在的数据）**：缓存空值（设短 TTL）、布隆过滤器前置过滤
   - **击穿（热点 key 过期）**：互斥锁保证只有一个请求回源、永不过期 + 异步刷新
   - **雪崩（大量 key 同时过期）**：TTL 加随机偏移、多级缓存、限流降级

# Cache-Aside 模式流程

```
读: 请求 → 查缓存 → 命中? → 返回
                    ↓ 未命中
                  查数据库 → 写缓存 → 返回
写: 请求 → 写数据库 → 删除缓存
```

# 缓存键设计

- 可读性：`user:profile:12345` 而非 `u_p_12345`
- 命名空间：用冒号分层级，`app:module:entity:id`
- 版本号：格式变更时在 key 中加版本标识
- 避免过长的 key（影响内存和网络传输）

# CDN 缓存策略

| 资源类型 | Cache-Control | 更新策略 |
|---------|---------------|---------|
| 打包后的 JS/CSS | `max-age=31536000, immutable` | 文件名含哈希，永久缓存 |
| 图片/字体 | `max-age=2592000` | 长缓存，更新时换文件名 |
| HTML | `max-age=0, must-revalidate` | 不缓存或短期缓存 |
| API 响应 | `max-age=60, s-maxage=300` | CDN 可缓���稍长 |

# 参考标准

- Redis 官方文档 —— 数据类型和配置指南
- HTTP Caching（RFC 7234）—— HTTP 缓存标准
- Google Web Fundamentals —— 缓存最佳实践
