---
name: owasp-top10
description: OWASP Top 10 安全漏洞详解 —— 2021 版十大 Web 安全风险、检查清单与修复方案
category: security
loading: on-demand
triggers:
  keywords: ["OWASP", "注入攻击", "XSS", "CSRF", "安全配置"]
---

# 这是什么

OWASP Top 10 是 Web 应用安全风险的权威排名，由全球安全专家共识产生。本 Skill 提供每项风险的详解、检测方法和修复方案。

# 何时使用

- 新功能上线前的安全审查
- 安全培训——团队内部安全意识教育
- 渗透测试前的自查
- 第三方安全审计准备
- 安全事件后的系统性修复

# 核心规则

1. **所有用户输入都是恶意的**：从 URL 参数到 HTTP Header 到文件上传，任何来自客户端的数据都不信任。
2. **安全配置是最容易赢的仗**：关闭调试模式、禁用默认账号、移除不必要的模块。几十项小配置累积成巨大防护。
3. **依赖已知漏洞最容易被忽视**：你代码写得再好，用了一个有 CVE 的库就全完。定期审计依赖和自动扫描是标配。
4. **访问控制失效是最常见的高危漏洞**：用户不应该能看到或操作不属于他们的资源。每个端点、每个对象都需要授权检查。
5. **日志不足让攻击者来去无踪**：入侵可能已经发生了几周，但你没有日志所以不知道。

# 十大风险速查

1. **A01 访问控制失效**
   - 问题：用户可以访问其他用户的资源（IDOR）
   - 修复：每个端点的每个资源操作都做所有权校验。禁止仅靠前端隐藏按钮来做权限控制。

2. **A02 加密失效**
   - 问题：敏感数据明文传输或弱加密
   - 修复：全站 HTTPS、密码用 bcrypt/argon2、敏感数据加密存储、禁用过期加密算法。

3. **A03 注入**
   - 问题：SQL/NoSQL/OS 命令/ LDAP 注入
   - 修复：参数化查询（唯一有效方法）、输入白名单校验、最小权限的数据库账号。

4. **A04 不安全的设计**
   - 问题：缺少威胁建模、过度信任客户端验证
   - 修复：设计阶段做威胁建模、安全设计模式（如"安全默认值"）、限制资源消耗。

5. **A05 安全配置错误**
   - 问题：调试模式开启、默认密码、不必要的功能启用、错误页泄露堆栈
   - 修复：硬化的默认配置、自动化配置检查、关闭详细错误信息。

6. **A06 易受攻击和过时的组件**
   - 问题：使用有已知漏洞的库版本
   - 修复：持续扫描依赖、自动化更新工具（Dependabot）、及时升级。

7. **A07 认证和授权失败**
   - 问题：弱密码策略、会话管理缺陷、无多因素认证
   - 修复：强制强密码、限制登录尝试、会话失效机制、关键操作二次认证。

8. **A08 软件和数据完整性故障**
   - 问题：不安全的反序列化、CI/CD 管道被篡改
   - 修复：完整性校验（签名/哈希）、信任的依赖源、代码签名。

9. **A09 安全日志和监控故障**
   - 问题：攻击无记录、无告警、日志可被篡改
   - 修复：集中日志管理、攻击检测告警、日志完整性保护。

10. **A10 SSRF（服务端请求伪造）**
    - 问题：服务端被诱导发起恶意请求到内部网络
    - 修复：验证目标 URL、禁止内网地址、限制协议（只允许 HTTP/HTTPS）。

# 安全审查 SOP

1. 获取源码和架构文档
2. 运行自动扫描工具（SAST + 依赖扫描）
3. 逐项对照 OWASP Top 10 人工审查
4. 手动验证高危漏洞的可利用性
5. 编写报告：漏洞、严重程度、复现步骤、修复建议
6. 修复后重新验证

# 注入攻击速查

| 类型 | 攻击向量 | 防御 |
|------|---------|------|
| SQL | `' OR '1'='1` | 参数化查询 |
| NoSQL | `{"$gt": ""}` | 输入类型校验 |
| OS 命令 | `; rm -rf /` | 不用 shell=True |
| XSS | `<script>alert(1)</script>` | 输出编码 |
| 路径遍历 | `../../etc/passwd` | 路径规范化 |

# 参考标准

- OWASP Top 10 2021 官方文档 —— 权威风险排名
- OWASP ASVS —— 应用安全验证标准
- OWASP 测试指南 —— 渗透测试方法论
- CWE Top 25 —— 最危险的软件弱点
