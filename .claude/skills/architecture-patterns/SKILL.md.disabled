---
name: architecture-patterns
description: 架构模式总览 —— 分层/六边形/微服务/事件驱动/CQRS/DDD 基础
category: architecture
loading: on-demand
triggers:
  keywords: ["架构设计", "架构模式", "architecture", "系统设计"]
---

# 这是什么

架构模式是经过验证的系统结构模板。选择合适的架构模式直接影响系统的可维护性、可扩展性和团队效率。

# 何时使用

- 新项目或新系统的架构选型
- 现有系统出现难以扩展的瓶颈，需要重构
- 从单体向分布式迁移
- 技术评审中需要论证架构决策
- 学习系统设计，理解不同模式的利弊

# 核心规则

1. **简单优先**：选择能解决问题的最简单架构。单体不是贬义词——如果你的团队只有 5 个人、用户量不过万，单体完全够用。
2. **架构由需求驱动**：不要因为"微服务很流行"就拆服务。业务复杂度、团队规模、可预期增长决定架构。
3. **边界清晰比模式最新更重要**：明确模块边界和接口契约比追求时髦的架构模式更能保证长期可维护性。
4. **先垂直切，再水平切**：按业务能力而非技术层划分模块。"用户模块"比"Controller 层 + Service 层"好。
5. **架构不是一成不变的**：好的架构适应变化。从单体到模块化单体到微服务是进化路径，不是一步到位。

# 工作流程

1. **需求分析**
   - 梳理核心业务能力和数据流
   - 识别变化频率不同的模块（频繁迭代 vs 稳定沉淀）
   - 评估非功能性需求：并发量、延迟要求、数据一致性级别
   - 了解团队能力和组织架构（Conway 定律）

2. **模式评估**
   - 列出候选架构模式
   - 从以下维度打分：开发效率、部署复杂度、可测试性、运维成本、团队适配
   - 做原型验证最不确定的部分
   - 确定当前阶段的架构 + 后续演化方向

3. **划分边界**
   - 识别限界上下文（Bounded Context）
   - 每个上下文内高内聚，上下文间低耦合
   - 定义上下文间的通信方式：同步 API、异步消息、共享数据库
   - 每个上下文有自己的数据模型，不跨上下文直接访问数据库

4. **文档化**
   - 架构决策记录（ADR）—— 每个架构决策记录上下文、选项、理由
   - C4 模型：系统上下文图、容器图、组件图、代码图四层递进
   - 架构图应自解释，标注通信方向、协议和数据格式

# 架构模式速查

| 模式 | 适合 | 不适合 |
|------|------|--------|
| 分层架构 | 简单 CRUD 应用 | 复杂业务逻辑 |
| 六边形架构 | 需要切换基础设施 | 简单项目（过度设计） |
| 微服务 | 大团队、高并发、多领域 | 小团队、简单业务 |
| 事件驱动 | 异步流程、多系统协作 | 强一致性要求的场景 |
| CQRS | 读写差异大、查询复杂 | 简单 CRUD（增加复杂度） |
| DDD | 复杂业务逻辑 | 纯 CRUD 应用 |

# 六边形架构（端口与适配器）

```
          ┌─────────────────────┐
 Web ──►  │  应用核心（纯逻辑）  │ ◄── 数据库
 CLI ──►  │  端口 = 接口定义     │ ◄── 消息队列
 测试 ──► │  适配器 = 具体实现   │ ◄── 外部 API
          └─────────────────────┘
```
核心不依赖任何外部技术。适配器是可替换的"插件"。

# 事件驱动架构

| 模式 | 描述 |
|------|------|
| 事件通知 | "订单已创建"——通知但不传数据 |
| 事件携带状态 | 事件包含处理所需的全部数据 |
| 事件溯源 | 存储事件而非当前状态，状态由事件重放派生 |
| CQRS | 读写分离，不同数据模型 |

# 参考标准

- 《设计数据密集型应用》（Martin Kleppmann）—— 分布式系统设计圣经
- 《领域驱动设计》（Eric Evans）—— DDD 经典
- 《软件架构：架构模式、特征及实践》（Mark Richards）—— 架构模式全览
- C4 模型 —— 架构可视化方法
