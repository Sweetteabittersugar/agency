---
name: auth-patterns
description: 认证授权模式 —— OAuth 2.0、JWT 设计、RBAC/ABAC 权限模型与 MFA 策略
category: security
loading: on-demand
triggers:
  keywords: ["认证", "授权", "OAuth", "JWT", "权限设计", "登录"]
---

# 这是什么

认证（你是谁）和授权（你能做什么）是应用安全的两个基石。本 Skill 涵盖主流认证协议、令牌设计、权限模型和多因素认证。

# 何时使用

- 设计新系统的登录和权限体系
- 从单体应用拆分为微服务，需要统一认证
- 接入第三方登录（Google、GitHub、微信）
- 需要从 Session 机制迁移到 JWT
- 合规需求要求增加 MFA

# 核心规则

1. **认证与授权分工明确**：认证回答"你是谁"，授权回答"你能做什么"。一个服务只做认证（签发令牌），各服务自己做授权（解析令牌+判断权限）。
2. **令牌无状态是目标，有状态是保障**：JWT 自包含信息减少查库，但必须配合短期有效期和刷新令牌。必要时加黑名单实现即时失效。
3. **最小权限原则**：只给完成当前操作所需的最小权限。定期审计权限。
4. **安全存储凭据**：密码用 bcrypt/argon2 哈希。绝不存储明文密码。令牌不在 URL 中传输。
5. **不要自己发明认证协议**：用 OAuth 2.0 / OIDC 等成熟标准。自定义加密和协议是安全灾难的常见来源。

# 工作流程

1. **认证方案选择**
   - **Session + Cookie**：传统方案，服务端维护状态。适合单体应用。
   - **JWT**：无状态令牌。适合分布式系统和 API 鉴权。
   - **OAuth 2.0 + OIDC**：第三方登录和授权。适合需要接入外部身份提供者的场景。
   - **API Key**：服务间调用。适合机器对机器的认证。

2. **密码管理**
   - 用户密码：`bcrypt` 哈希（cost factor >= 10），或用 `argon2id`
   - 密码强度：最少 8 位，建议用 entropy 而非复杂规则
   - 密码重置：生成一次性令牌，过期时间 15-30 分钟，用完即废弃
   - 登录锁定：连续失败 5 次后锁定 15 分钟（或 CAPTCHA）

3. **JWT 设计**
   - 载荷精简：只放 user_id、role、exp。不放敏感信息。
   - 短期有效：access token 15 分钟，refresh token 7 天
   - 签名算法：优先 `RS256`（非对称）或至少 `HS256`
   - 不在 localStorage 存储令牌（XSS 风险），用 httpOnly Secure Cookie

4. **权限模型**
   - **RBAC（基于角色）**：角色 → 权限。`admin` 角色有 `user:delete` 权限。适合大多数应用。
   - **ABAC（基于属性）**：用户属性 + 资源属性 + 环境属性 → 决策。更灵活但更复杂。
   - **ReBAC（基于关系）**：关注对象间的关系（Google Zanzibar）。适合社交和协作场景。
   - 选择原则：先用 RBAC，当角色爆炸（超过 20 个角色）时考虑 ABAC

5. **MFA 策略**
   - 优先用 TOTP（Google Authenticator）或 WebAuthn（硬件密钥/Face ID）
   - SMS 作为最后选择（SIM swap 攻击风险）
   - 敏感操作强制 MFA：修改密码、修改邮箱、大额交易
   - 记住设备功能：信任设备 30 天，减少用户摩擦

# OAuth 2.0 授权流程选择

| 流程 | 适用场景 | 安全性 |
|------|---------|--------|
| Authorization Code + PKCE | SPA、移动端 | 最高 |
| Client Credentials | 服务间调用 | 高 |
| Resource Owner Password | 已弃用，避免使用 | 低 |
| Implicit | 已弃用，避免使用 | 低 |

# JWT 常见漏洞与修复

| 漏洞 | 修复 |
|------|------|
| `alg: none` 绕过签名 | 服务端只接受 `RS256` 或 `HS256` |
| 密钥泄露 | 定期轮换密钥，用非对称算法 |
| 过期时间过长 | Access Token 15 分钟以内 |
| 敏感信息泄露 | 不在 JWT 中放密码、信用卡号等 |
| 随意接受 issuer | 验证 `iss` 字段 |

# 参考标准

- OAuth 2.0（RFC 6749）—— 授权框架
- OIDC（OpenID Connect）—— 基于 OAuth 2.0 的认证层
- JWT（RFC 7519）—— JSON 令牌标准
- NIST SP 800-63B —— 数字身份认证指南
