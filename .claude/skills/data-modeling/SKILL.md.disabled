---
name: data-modeling
description: 数据建模方法论 —— ER 建模、星型/雪花模式、数据仓库设计与维���建模
category: data
loading: on-demand
triggers:
  keywords: ["数据建模", "ER图", "数据仓库", "data model"]
---

# 这是什么

数据建模是将业务需求转化为数据结构的系统化过程。好的数据模型使查询高效、业务变更成本低、数据含义清晰。

# 何时使用

- 设计新系统的数据层
- 搭建数据仓库或数据集市
- 遗留系统的数据层重构
- 多个数据源需要统一视图
- 复杂报表需求的底层设计

# 核心规则

1. **业务驱动模型**：先理解业务流程和查询需求，再建模。不先画 ER 图再看业务怎么办。
2. **正确的范式取决于场景**：OLTP 用 3NF 减少冗余；OLAP 用星型模式优化查询。没有一种范式适合所有场景。
3. **命名即文档**：表名、列名应自解释。`user_id` 而不是 `id`；`created_at` 而不是 `crt_tm`。
4. **数据类型选择影响性能**：用最小能满足需求的类型（`TINYINT` 而非 `INT` 存状态）。类型转换是查询性能杀手。
5. **时间维度是所有分析的基础**：每个事实表几乎都有时间戳。提前设计时间维度表让分析查询大幅简化。

# 工作流程

1. **概念建模**
   - 与业务方沟通，梳理核心业务实体
   - 绘制高层次 ER 图（实体、关系、基数）
   - 确认关键业务规则（一个订单只能属于一个用户等）
   - 不要陷入字段细节——概念建模聚焦"是什么"而非"怎么存"

2. **逻辑建模（OLTP）**
   - 规范化到 3NF
   - 定义主键、外键和约束
   - 考虑索引策略（哪些查询最重要）
   - 设计审计字段：`created_at`、`updated_at`、`version`（乐观锁）

3. **逻辑建模（OLAP）**
   - 识别业务过程（销售、注册、支付）→ 事实表
   - 识别描述维度（时间、用户、产品、地区）→ 维度表
   - 选择星型模式（维度表直接连事实表）还是雪花模式（维度表可以进一步规范化）
   - 事实表的粒度至关重要——一行代表什么？

4. **物理建模**
   - 表分区策略（按时间范围分区最常用）
   - 实体关系实现（一对一、一对多通过外键，多对多通过关联表）
   - 继承关系实现（单表继承、类表继承、具体表继承）
   - 物化视图预计算常见聚合查询

5. **模型验证**
   - 用真实查询场景验证模型能否高效支持
   - 建原型表并插入采样数据
   - 执行核心查询看执行计划是否合理
   - 与开发团队评审模型

# 维度建模模式

| 模式 | 结构 | 适合场景 |
|------|------|---------|
| 星型 | 事实表 + 一层维度表 | 大多数分析场景 |
| 雪花 | 维度表再规范化 | 维度属性多变 |
| 事实星座 | 多个事实表共享维度表 | 多业务过程分析 |
| 累积快照 | 记录业务过程各阶段时间 | 订单生命周期 |

# 数据仓库分层

```
ODS (操作数据层) — 原始数据，与源系统一致
  ↓
DWD (明细数据层) — 清洗、标准化后的明细数据
  ↓
DWS (汇总数据层) — 轻度聚合（日/周汇总）
  ↓
ADS (应用数据层) — 面向特定业务的数据产品
```

# 常见建模错误

| 错误 | 后果 | 改进 |
|------|------|------|
| 过度规范化 | 分析查询需要 JOIN 十几张表 | OLAP 适当反规范化 |
| 字段含义模糊 | `status` 是哪种状态？ | 枚举或外键关联状态表 |
| 用字符串存时间 | 索引低效、排序错误 | 用 DATETIME/TIMESTAMP |
| 缺少时间戳 | 无法追踪数据变更 | 所有表加时间戳字段 |
| 热冷数据混存 | 查询全表扫描 | 按时间范围分区 |

# 参���标准

- 《数据仓库工具箱》（Ralph Kimball）—— 维度建模经典
- ER 图规范（Chen 表示法 / Crow's Foot）—— 实体关系建模符号
- 《数据模型资源手册》（Len Silverston）—— 通用数据模型参考
