---
name: rollback-strategy
description: 回滚策略方法论 —— 蓝绿部署、金丝雀发布、数据兼容与回滚决策
category: devops
loading: on-demand
triggers:
  keywords: ["回滚", "rollback", "蓝绿", "金丝雀", "发布策略"]
---

# 这是什么

回滚策略是在部署失败或出现线上问题时，快速将系统恢复到上一个已知稳定版本的方法。回滚能力直接影响故障恢复时间（MTTR）。

# 何时使用

- 设计 CI/CD 部署流程
- 制定版本发布和回滚 SOP
- 数据库迁移需要可逆方案
- 微服务架构下需要协调多服务回滚
- 事故后复盘需要优化回滚流程

# 核心规则

1. **回滚必须比部署更快**：如果部署需要 5 分钟，回滚应控制在 1 分钟以内。自动化程度决定回滚速度。
2. **数据库变更可逆是前提**：破坏性的数据库变更（删除列、修改类型）必须分两步发布——先兼容（新增列），确认稳定后再清理。
3. **蓝绿优于原地升级**：保留完整旧环境（蓝），切换流量到新环境（绿）。有问题秒级切回，不留残留。
4. **回滚是常练的技能**：不在事故时才第一次回滚。定期演练，确保流程有效、人员熟练。
5. **不要回滚数据**：回滚代码，不回滚数据。数据应该有向前兼容的保护。

# 工作流程

1. **发布前准备**
   - 确认回滚按钮可用——CI/CD 平台的一键回滚能力
   - 数据库迁移脚本同时准备回滚脚本
   - 记录发布版本号、发布时间、变更内容
   - 确定回滚决策标准（错误率阈值、响应时间阈值）

2. **部署策略选择**
   - **蓝绿部署**：两套完整环境切换。回滚=切回旧环境。最简单可靠。
   - **金丝雀发布**：先 5% 流量验证，逐步提升。发现问题只影响小部分用户。
   - **滚动更新**：逐台替换。回滚需逐台回退。成本低但回滚慢。
   - **影子发布**：复制真实流量到新版本但不影响用户。验证性能后正式切换。

3. **回滚决策**
   - 触发条件：错误率 > 1%、P99 延迟 > 2x 基线、收入下降、关键流程中断
   - 决策权：值班工程师可直接触发回滚（不需要审批链），事后报告
   - 黄金时间窗口：问题发现后 5 分钟内决策，10 分钟内完成回滚

4. **数据库回滚策略**
   - **可逆迁移**：新增列/表——回滚脚本删除即可
   - **不可逆迁移**：删除列/表——分两步发布。第一步标记弃用（仍兼容），第二步（下个版本）真正删除
   - **数据迁移**：必须保留原始数据直到确认新版本稳定
   - 所有迁移脚本在 Git 中对应一个回滚脚本（`up.sql` + `down.sql`）

5. **事后复盘**
   - 记录回滚原因和触发条件
   - 评估回滚耗时是否符合 SLA
   - 改进预防措施（加强测试、金丝雀阶段延长）
   - 更新回滚手册中的经验教训

# 部署策略对比

| 策略 | 回滚速度 | 成本 | 用户影响范围 | 适合场景 |
|------|---------|------|-------------|---------|
| 蓝绿 | 秒级 | 双倍资源 | 无 | 核心服务 |
| 金丝雀 | 分钟级 | 略高于单套 | 小部分用户 | 高频迭代 |
| 滚动 | 分钟级 | 常规 | 部分用户 | 传统应用 |
| 原地 | 不可靠 | 常规 | 全部用户 | 不推荐 |

# 回滚检查清单

- [ ] 回滚脚本是否已测试？
- [ ] 数据库迁移是否有对应的回滚脚本？
- [ ] API 是否向后兼容（旧客户端能否正常工作）？
- [ ] 新版本写入的数据是否与旧版本兼容？
- [ ] 回滚后是否需要清理新增的配置项或缓存？
- [ ] 回滚后消息队列中的消息格式是否兼容？

# 向后兼容原则

- **API 只加不删**：新增字段安全，删除字段危险
- **默认值兜底**：新字段必须有默认值，旧版本不认识新字段时不崩溃
- **容忍未知字段**：JSON 解析时忽略未知字段
- **数据库列只加不删**：删除列用两步法

# 参考标准

- Martin Fowler 蓝绿部署 —— 零停机部署模式
- K8s 金丝雀部署指南 —— 渐进式发布
- 《发布！》（Jez Humble）—— 持续交付中的部署策略
