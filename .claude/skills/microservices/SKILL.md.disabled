---
name: microservices
description: 微服务架构方法论 —— 服务拆分原则、通信模式、数据一致性与服务治理
category: architecture
loading: on-demand
triggers:
  keywords: ["微服务", "microservice", "服务拆分", "分布式"]
---

# 这是什么

微服务架构将单体应用拆分为一组小型、自治的服务。每个服务围绕业务能力构建，独立部署、独立伸缩。

# 何时使用（和何时不用）

**适合微服务：**
- 团队规模 > 20 人，需要独立迭代
- 不同模块有不同的伸缩需求（搜索需要 10 台，用户中心只需要 2 台）
- 需要独立部署不同模块（每周多次 vs 每月一次）
- 多业务领域需要不同的技术栈

**不适合微服务：**
- 团队 < 5 人——协调成本超过收益
- 业务逻辑简单——拆分是过度设计
- 强一致性需求——分布式事务增加巨大复杂度
- 没有 DevOps 能力——微服务需要自动化部署和监控

# 核心规则

1. **先打磨单体，再拆分**：在单体中先建立清晰的模块边界。当模块边界稳定后再考虑物理拆分。
2. **围绕业务能力组织**：每个微服务对应一个业务能力，而不是技术层。`订单服务` 不是 `订单数据库服务 + 订单业务服务`。
3. **每个服务有独立数据库**：不跨服务共享数据库。数据共享通过 API 或事件，而非直接查表。
4. **异步通信优先**：同步调用产生强依赖和级联故障。用事件驱动降低服务耦合。
5. **接受最终一致性**：分布式系统中强一致性代价极高。大部分场景下，秒级的最终一致性完全可接受。

# 工作流程

1. **服务拆分**
   - 从限界上下文出发（DDD 方法）
   - 按变化频率拆分：稳定模块和快速迭代模块分开
   - 按性能特征拆分：高并发模块独立伸缩
   - 先拆读（查询服务），再拆写（命令服务）
   - 每次只拆一个服务，验证稳定性后再拆下一个

2. **通信模式**
   - **同步（请求/响应）**：REST/gRPC——适合需要即时响应的场景
   - **异步（消息）**：消息队列/事件流——适合可以接受延迟的场景
   - **混合**：用户请求走同步、后台任务走异步——最常用

3. **数据一致性**
   - **Saga 模式**：长事务拆成多个本地事务 + 补偿操作
   - **事件溯源**：事件作为真相来源，各服务消费事件更新自己的读模型
   - **出站模式（Outbox Pattern）**：在同一事务中写数据库和发消息，保证二者原子性
   - **两阶段提交**：尽量不用——性能差、单点故障风险高

4. **服务治理**
   - **服务发现**：服务实例动态注册和发现（Consul、K8s Service）
   - **负载均衡**：客户端负载均衡或服务网格（Istio/Linkerd）
   - **熔断**：下游故障时快速失败，而非长时间等待（Hystrix/Resilience4j）
   - **API 网关**：统一入口，认证、限流、路由

5. **可观测性**
   - **日志**：结构化日志，每条包含 trace_id
   - **分布式追踪**：Jaeger/Zipkin 追踪请求在服务间的路径
   - **指标**：每个服务暴露延迟、错误率、请求量
   - **健康检查**：Liveness + Readiness 探针

# 服务拆分反模式

| 反模式 | 问题 | 改进 |
|--------|------|------|
| 按技术层拆分 | 一个变更涉及所有服务 | 按业务能力拆分 |
| 共享数据库 | 破坏数据封装 | 每服务独立数据库 |
| 过小的服务 | 10 行代码一个服务 | 每个服务有足够的业务价值 |
| 分布式单体 | 拆了但强依赖，一个服务故障全挂 | 引入异步和容错机制 |
| 没有自动化 | 手工部署 N 个服务 | 先建 CI/CD 再拆 |

# 微服务成熟度模型

- **Level 1**：模块化单体——代码模块清晰但部署在一起
- **Level 2**：首批拆分——2-3 个独立服务，已验证通信和部署
- **Level 3**：全面微服务——大部分功能独立部署，有服务治理
- **Level 4**：Serverless/FaaS——进一步拆分为函数粒度

# 参考标准

- 《构建微服务》（Sam Newman）—— 微服务设计经典
- 《微服务架构设计模式》（Chris Richardson）—— 模式目录
- K8s 官方微服务指南 —— 容器编排最佳实践
- Martin Fowler 微服务文章 —— 微服务定义和先决条件
