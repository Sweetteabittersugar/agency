---
name: unit-test-patterns
description: 单元测试模式 —— AAA 模式、边界值分析、等价类划分与 Mock 最佳实践
category: testing
loading: on-demand
triggers:
  keywords: ["单元测试", "unit test", "断言", "覆盖率"]
---

# 这是什么

单元测试是验证代码最小可测单元（函数、方法、类）正确性的测试方法。本 Skill 提供系统化的测试编写模式和最佳实践。

# 何时使用

- 编写新功能的单元测试
- 重构前补充测试作为安全网
- 提高测试质量（从"有测试"到"好测试"）
- Bug 修复后添加回归测试
- 团队需要统一测试规范

# 核心规则

1. **测试行为，不测试实现**：验证函数做了什么（输入 → 输出），而不是它内部怎么做的。实现重构时测试不该变化。
2. **AAA 模式（Arrange-Act-Assert）**：每个测试明确划分三个步骤——准备数据、执行操作、验证结果。
3. **一个测试只验证一个行为**：不要把五个场景塞进一个测试。测试失败时应能立即定位问题。
4. **FIRST 原则**：快速（Fast）、独立（Independent）、可重复（Repeatable）、自验证（Self-validating）、及时（Timely）。
5. **覆盖率是下限，不是上限**：80% 是底线。更重要的是覆盖关键路径和边界条件。

# 工作流程

1. **确定测试范围**
   - 公共 API（公开的函数和方法）— 必须测试
   - 关键业务逻辑 — 必须测试
   - 边界条件和异常路径 — 必须测试
   - 简单的 getter/setter — 可以跳过
   - 框架代码和配置 — 通常不需要测试

2. **遵循 AAA 模式**
   ```
   // Arrange: 准备测试数据和依赖
   const user = { id: 1, balance: 100 };
   // Act: 执行被测操作
   const result = withdraw(user, 30);
   // Assert: 验证结果
   expect(result.balance).toBe(70);
   ```

3. **边界值分析**
   - 有效范围的边界：最小值、最大值、刚好在范围内的值
   - 有效范围外的边界：刚好小于最小值、刚好大于最大值
   - 空输入：null、undefined、空字符串、空数组
   - 特殊值：0、负数、极大值、Unicode 字符

4. **等价类划分**
   - 将输入数据按等价性分组，每组选一个代表值测试
   - 有效等价类：正常使用场景
   - 无效等价类：应触发校验错误的场景
   - 示例：年龄（0-150）→ 有效类：25；无效类：-1、151、"abc"

5. **Mock 使用原则**
   - 只 Mock 外部依赖（数据库、API、文件系统）
   - 不 Mock 被测对象的内部方法
   - 不 Mock 值对象和 DTO
   - Mock 的返回值应模拟真实行为，不是无意义的固定值

6. **测试命名**
   - `test_<被测函数>_<场景>_<预期结果>`
   - 示例：`test_withdraw_sufficientBalance_returnsReducedBalance`
   - 测试名应描述"发生了什么"和"应该怎样"

# 断言最佳实践

| 断言类型 | 示例 |
|----------|------|
| 精确值 | `assertEquals(expected, actual)` |
| 真假 | `assertTrue(condition)` / `assertFalse(condition)` |
| 异常 | `assertThrows(TypeError, () => {...})` |
| 近似值 | `assertAlmostEqual(0.3, 0.1 + 0.2, places=2)` |
| 包含 | `assertContains(element, collection)` |

# 测试反模式

| 反模式 | 示例 | 改进 |
|--------|------|------|
| 测试实现细节 | 验证内部私有方法的行为 | 通过公共接口验证行为 |
| 过度 Mock | Mock 了所有参数，测试只验证 Mock 调用 | Mock 只用于外部依赖 |
| 脆弱的测试 | 依赖数据库的特定数据顺序 | 每个测试自己准备数据 |
| 测试间有依赖 | test_2 依赖 test_1 的执行结果 | 每个测试独立运行 |
| 假阳性 | 测试通过了但实际功能有问题 | 确保断言充分验证结果 |

# 测试数据管理

- **工厂函数**：封装对象创建逻辑，测试只传差异参数
- **测试夹具（Fixtures）**：公共的测试前准备，框架自动调用
- **参数化测试**：同一个测试逻辑，多组输入数据，减少重复代码
- **快照测试**：将复杂输出保存为快照文件，后续对比差异

# 参考标准

- xUnit 测试模式（Gerard Meszaros）—— 测试模式经典
- FIRST 原则 —— 优秀单元测试的五个特征
- 《单元测试的艺术》（Roy Osherove）—— 测试实践指南
