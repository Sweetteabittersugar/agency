---
name: database-design
description: 数据库设计方法论 —— 范式设计、索引策略、分库分表与读写分离
category: architecture
loading: on-demand
triggers:
  keywords: ["数据库设计", "建表", "索引", "schema", "DDL"]
---

# 这是什么

数据库设计是定义数据存储结构、关系和访问模式的工程实践。好的数据库设计直接影响查询性能、数据一致性和系统可扩展性。

# 何时使用

- 新项目或新模块的数据库建模
- 查询性能出现瓶颈，需要优化索引
- 数据量增长导致单表过大
- 需要从单体数据库向分布式演进
- 数据模型评审或变更

# 核心规则

1. **模型驱动设计**：先理解业务领域和数据关系，再设计表结构。不要先建表再想业务。
2. **规范化与反规范化的平衡**：OLTP 场景追求 3NF 减少数据冗余；OLAP 场景适当反规范化以加速查询。
3. **索引是为查询服务的**：每个索引必须对应具体的查询模式。没有查询使用的索引是纯负担（占用存储、拖慢写入）。
4. **主键设计影响深远**：分布式场景优先用 UUID 或 Snowflake 等分布式 ID，避免自增主键的热点问题。
5. **数据生命周期管理**：设计时就考虑数据归档和删除策略。不要等表涨到几千万行再想怎么办。

# 工作流程

1. **概念建模**
   - 识别业务域的核心实体（用户、订单、商品）
   - 定义实体间关系：一对一、一对多、多对多
   - 确定每个实体的核心属性
   - 绘制 ER 图，与业务方确认理解一致

2. **逻辑设计（范式化）**
   - 第一范式（1NF）：每列原子化，无重复组
   - 第二范式（2NF）：非主键列完全依赖于主键
   - 第三范式（3NF）：非主键列不传递依赖于主键
   - OLTP 场景至少达到 3NF
   - 识别需要反规范化的热点查询

3. **物理设计**
   - 选择合适的数据类型（最小满足需求的类型）
   - 设计主键：自增（单机）、UUID（分布式）、Snowflake（高并发）
   - 设计索引：主键索引、唯一索引、普通索引、联合索引
   - 合理使用约束：NOT NULL、UNIQUE、CHECK、FOREIGN KEY

4. **索引设计**
   - **最左前缀原则**：联合索引 `(a, b, c)` 覆盖查询 `WHERE a=?` 和 `WHERE a=? AND b=?`
   - **覆盖索引**：查询列都在索引中，无需回表
   - **避免索引失效**：函数包裹列、前导模糊查询 `LIKE '%x'`、隐式类型转换
   - **选择性原则**：索引列的值越分散效果越好。性别列建索引几乎无意义

5. **扩展性设计**
   - 垂直拆分：按业务模块拆到不同数据库
   - 水平拆分：按用户 ID 或时间范围分表分库
   - 读写分离：主库写入，从库读取
   - 缓存层：Redis 前置，减轻数据库压力

# 数据类型选择指南

| 场景 | 推荐类型 | 避免 |
|------|---------|------|
| 状态字段 | TINYINT / ENUM | VARCHAR |
| 时间 | TIMESTAMP / DATETIME | INT 存储时间戳 |
| 金额 | DECIMAL(18,2) | FLOAT / DOUBLE |
| 长文本 | TEXT / JSON | VARCHAR 存超长内容 |
| 布尔 | BOOLEAN / TINYINT(1) | VARCHAR 存 "true"/"false" |
| UUID | CHAR(36) / BINARY(16) | VARCHAR(255) |

# 索引使用检查清单

- [ ] WHERE 条件列是否有索引？
- [ ] JOIN 关联列是否有索引？
- [ ] ORDER BY / GROUP BY 列是否有索引？
- [ ] 联合索引的列顺序是否符合查询模式？
- [ ] 是否有未使用的索引需要删除？
- [ ] 是否使用了覆盖索引避免回表？

# 分库分表策略

| 策略 | 优点 | 缺点 |
|------|------|------|
| 按范围分 | 扩容简单 | 热点不均匀 |
| 按哈希分 | 分布均匀 | 扩容需数据迁移 |
| 按业务分 | 隔离性好 | 跨库查询困难 |
| 混合策略 | 兼顾以上 | 复杂度高 |

# 参考标准

- 数据库规范化理论（Codd）—— 1NF 到 5NF
- MySQL 索引优化文档 —— 索引工作原理
- PostgreSQL 官方设计指南 —— 物理设计最佳实践
- 《高性能 MySQL》—— 索引和查询优化经典
