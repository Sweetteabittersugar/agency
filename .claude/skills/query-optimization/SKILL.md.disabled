---
name: query-optimization
description: 查询优化方法论 —— 执行计划分析、索引优化、慢查询诊断与 SQL 重构
category: data
loading: on-demand
triggers:
  keywords: ["查询优化", "SQL优化", "执行计划", "慢查询"]
---

# 这是什么

查询优化是分析、诊断和改善 SQL 查询性能的系统方法。目标是减少查询时间、降低资源消耗，从秒级优化到毫秒级。

# 何时使用

- 查询响应时间超过业务 SLA
- 数据库 CPU 使用率居高不下
- 监控平台报告大量慢查询
- 数据量增长后原本正常的查询变慢
- 需要设计高效的报表查询

# 核心规则

1. **执行计划是一切的起点**：不看 EXPLAIN，一切优化都是猜测。执行计划精确告诉你在哪里花费了时间。
2. **索引直达，不要扫表**：目标是把 `type` 从 `ALL`（全表扫描）优化到 `ref`（索引查找）或更低。每一级提升都是数量级的性能改善。
3. **减少数据量，而非增加算力**：用 WHERE 提前过滤、用 LIMIT 限制返回行数、只 SELECT 需要的列而非 `SELECT *`。减少传输和计算的数据量比升级硬件更有效。
4. **JOIN 的顺序和类型至关重要**：先过滤小表，再关联大表。嵌套循环 JOIN 适合小表驱动大表，HASH JOIN 适合两张大表。
5. **不是所有的慢都需要优化**：如果查询每天只跑一次且用户接受等 5 秒，不需要花两天优化到 0.1 秒。优化投入应与收益匹配。

# 工作流程

1. **慢查询定位**
   - 开启慢查询日志：`long_query_time = 0.5`（超过 0.5 秒记录）
   - 用 `pt-query-digest` 或数据库自带工具分析慢查询日志
   - 按"总耗时"而非"单次耗时"排序——高频慢查询优先优化
   - 定位最耗时的 5 条查询

2. **执行计划分析**
   - `EXPLAIN SELECT ...` 查看执行计划
   - 关键字段：`type`（访问类型）、`rows`（扫描行数）、`Extra`（额外信息）
   - `type` 从优到差：`const` > `eq_ref` > `ref` > `range` > `index` > `ALL`
   - `Extra: Using filesort` 和 `Using temporary` 是需要优化的信号
   - `rows` 应接近实际返回行数，差距大说明索引选择性差

3. **索引优化**
   - 为 WHERE / JOIN / ORDER BY / GROUP BY 列创建索引
   - 联合索引遵循最左前缀原则
   - 用覆盖索引消除回表：`Using index` 比 `Using where` 好
   - 删除重复和未使用的索引（它们拖慢写入）
   - 索引不是越多越好——每个索引都占用磁盘和影响写入性能

4. **SQL 重写**
   - 用 `EXISTS` 替代 `IN` 子查询（子查询小用 IN，外层小用 EXISTS）
   - 避免 WHERE 条件中对列进行函数操作（`WHERE DATE(created_at) = '2024-01-01'` 无法用索引）
   - 用 `UNION ALL` 替代 `UNION`（避免去重排序）
   - 大偏移量分页用"基于上一次位置"的游标分页替代 `OFFSET`
   - 避免 `SELECT *`，只取需要的列

5. **验证与监控**
   - 优化前后对比 EXPLAIN 输出
   - 在测试环境用真实数据量验证性能提升
   - 监控上线后的慢查询数量和 CPU 使用率
   - 持续跟踪核心查询的执行计划（索引可能随时间退化）

# EXPLAIN 关键指标

| 字段 | 含义 | 理想值 |
|------|------|--------|
| type | 访问类型 | const / eq_ref / ref / range |
| key | 使用的索引 | 非 NULL |
| rows | 预估扫描行数 | 尽量小 |
| filtered | WHERE 过滤后剩余比例 | 尽量高 |
| Extra | 额外操作 | 避免 filesort、temporary |

# SQL 反模式

| 反模式 | 问题 | 改进 |
|--------|------|------|
| `SELECT *` | 传输多余数据，无法用覆盖索引 | 明确列名 |
| `WHERE YEAR(date) = 2024` | 索引失效 | `WHERE date >= '2024-01-01' AND date < '2025-01-01'` |
| `LIMIT 100000, 20` | 扫描 100020 行返回 20 行 | 游标分页 `WHERE id > last_id LIMIT 20` |
| `OR` 条件跨多列 | 部分条件无法用索引 | 拆为 `UNION ALL` |
| `IS NOT NULL` 在大数据量 | 索引选择性差 | 设计时避免 NULL，用默认值 |

# 参考标准

- MySQL 查询优化文档 —— EXPLAIN 详细解读
- 《高性能 MySQL》—— 查询优化经典
- PostgreSQL 性能优化指南 —— 统计信息和查询规划器
- 《SQL 反模式》（Bill Karwin）—— 常见 SQL 设计错误
