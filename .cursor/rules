# everythingclaudecode Cursor Rules

将这些规则复制到 Cursor 的项目配置中。Cursor 支持 `.cursorrules` 文件（项目根目录）或 `.cursor/rules/` 目录。

## 项目级配置

在项目根目录创建 `.cursorrules` 文件，包含以下内容：

```
# 从 everythingclaudecode 导入的工程规范

## 代码风格
- 禁止修改已有对象，始终创建新对象（不可变性）
- 文件控制在 200-400 行，最多 800 行
- 函数控制在 50 行以内
- 嵌套不超过 4 层
- 使用 snake_case（Python）/ camelCase（TypeScript）
- 所有函数必须有类型注解（Python）/ 类型声明（TypeScript）

## 错误处理
- 显式处理所有错误，不静默吞异常
- 错误信息对用户友好，日志记录详细上下文
- 使用 try-catch（JS/TS）或 try-except（Python）包裹异步操作

## 输入验证
- 所有用户输入必须验证后才能处理
- Python 使用 dataclass + 类型检查
- TypeScript 使用 Zod schema
- 不信任外部数据（API 响应、文件内容、用户输入）

## 安全
- 不在代码中硬编码密钥、token、密码
- 使用环境变量管理敏感配置
- 启动时验证必需的环境变量是否存在
- 所有 API 端点需要认证/授权检查

## 测试
- 目标覆盖率 80%+
- 新功能先写测试
- Python 使用 pytest，TypeScript 使用 vitest/jest
- 运行测试时开启 race detector（Go）/ 并行模式（Python）

## Git 工作流
- 提交格式：<type>: <description>（feat/fix/refactor/docs/test/chore）
- PR 前分析完整 commit 历史
- 新分支推送使用 git push -u origin <branch>
```

## 语言特定规则

### Python（追加到 .cursorrules）

```
## Python 特定
- 遵循 PEP 8
- 所有函数签名使用类型注解
- 优先使用 frozen dataclass 和 NamedTuple（不可变）
- 使用 Protocol 定义接口（鸭子类型）
- 上下文管理器管理资源
- 生成器处理大数据集
- 格式化：black + isort
- 检查：ruff
```

### TypeScript/JavaScript（追加到 .cursorrules）

```
## TypeScript 特定
- 优先使用 interface 而非 type（需要时）
- 不可变更新使用展开运算符
- 异步操作用 async/await + try-catch
- 使用 Zod 做运行时验证
- 控制台日志禁止出现在生产代码中（用 logger）
```

### Go（追加到 .cursorrules）

```
## Go 特定
- gofmt 和 goimports 必须使用
- 接受接口，返回结构体
- 保持接口精简（1-3 个方法）
- 错误使用 fmt.Errorf 包装上下文
- 使用 functional options 模式
- 始终使用 context.Context 控制超时
- 运行 go test -race ./... 检测竞态
```

## Cursor 目录方式（推荐）

在项目下创建 `.cursor/rules/` 目录，每个规则一个文件：

```
.cursor/rules/
├── coding-style.mdc     # 代码风格
├── testing.mdc          # 测试规范
├── security.mdc         # 安全规范
├── git-workflow.mdc     # Git 工作流
├── python.mdc           # Python 特定
├── typescript.mdc       # TypeScript 特定
└── golang.mdc           # Go 特定
```

每个 `.mdc` 文件格式：

```markdown
---
description: "代码风格规范"
globs: "*.py,*.ts,*.tsx,*.go,*.js"
alwaysApply: true
---

# 代码风格规范

[规则内容...]
```

## 快速安装

```bash
# 从 everythingclaudecode 复制规则到 Cursor 项目
mkdir -p .cursor/rules

# 复制通用规则
for f in coding-style testing security git-workflow; do
  cp everythingclaudecode/rules/common/$f.md .cursor/rules/$f.mdc
done

# 复制语言规则（按需）
cp everythingclaudecode/rules/python/*.md .cursor/rules/
cp everythingclaudecode/rules/typescript/*.md .cursor/rules/
cp everythingclaudecode/rules/golang/*.md .cursor/rules/
```

## 注意事项

- Cursor 不支持 Claude Code 的 hooks 系统，SessionStart/PostToolUse 等需要手动执行
- Maestro 调度引擎（dispatch.py）在 Cursor 中不可用，Agent 管理由 Cursor 自己的系统处理
- 费用追踪脚本（cost-tracker.py、cost-analyzer.py）可独立运行，不受影响
- 规则文件中的 `@agent` 引用在 Cursor 中无效，需移除或替换
